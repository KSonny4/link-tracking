version: v1beta1
name: buf.build/ksonny4/link-tracking
build:
  roots:
    - proto
lint:
  use:
    - DEFAULT
  ignore_only:
    PACKAGE_DIRECTORY_MATCH:
      - link-tracking.proto
    PACKAGE_VERSION_SUFFIX:
      - link-tracking.proto
    RPC_REQUEST_RESPONSE_UNIQUE:
      - link-tracking.proto
    RPC_RESPONSE_STANDARD_NAME:
      - link-tracking.proto
deps:
  - buf.build/googleapis/googleapis
  - buf.build/grpc-ecosystem/grpc-gateway
